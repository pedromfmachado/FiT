<div class="span14">
  <h1>Modalidades de Pagamento</h1>

  <table>
    <tr>
      <th>Nome</th>
      <th>Preço</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>

  <% @modalidade_pagamentos.each do |modalidade_pagamento| %>
    <tr>
      <td><%= modalidade_pagamento.nome %></td>
      <td><%= modalidade_pagamento.preco %> euros</td>
      <td><%= link_to 'Ver', modalidade_pagamento %></td>
      <td><%= link_to 'Editar', edit_modalidade_pagamento_path(modalidade_pagamento) %></td>
      <td><%= link_to 'Apagar', modalidade_pagamento, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
  </table>

  <div class="actions topSpacing1">
    <%= link_to 'Criar Modalidade Pagamento', new_modalidade_pagamento_path, :class => "btn primary" %>
  </div>
</div>
